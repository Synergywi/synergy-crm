# keep host.json and function.json
local.settings.json
test
tests
__tests__
coverage
.git
.gitignore
.vscode
# ignore sources & maps in the zip (compiled JS in dist is what runs)
**/*.ts
**/*.map
# never exclude dist or node_modules
