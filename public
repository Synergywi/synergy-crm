/* HubSpot-like light theme (orange/teal + navy text) */

/* Palette */
:root {
  --bg: #f7f9fc;
  --panel: #ffffff;
  --border: #dfe3eb;

  --text: #33475b;        /* HubSpot navy */
  --text-muted: #516f90;

  --primary: #ff7a59;     /* HubSpot orange */
  --primary-ink: #ffffff;

  --accent: #00b39f;      /* HubSpot teal */
}

/* ----- Global layout & typography ----- */
html, body {
  background: var(--bg) !important;
  color: var(--text) !important;
  font-family: ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, Inter, Arial;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

.hub-app {
  background: var(--bg) !important;
  color: var(--text) !important;
}

/* ----- Sidebar ----- */
.hub-app .sidebar {
  background: var(--panel) !important;
  color: var(--text) !important;
  border-right: 1px solid var(--border) !important;
  padding: 16px 14px;
  min-width: 240px;
}

.hub-app .logo {
  font-weight: 700;
  color: var(--text);
  margin-bottom: 12px;
}

/* Nav */
.hub-app .nav a {
  display: block;
  padding: 10px 12px;
  margin: 2px 0;
  color: var(--text-muted) !important;
  border-radius: 8px;
  text-decoration: none;
  transition: background .15s ease, color .15s ease;
}

.hub-app .nav a:hover {
  background: #eef3f8;
  color: var(--text) !important;
}

.hub-app .nav a.active {
  color: var(--text) !important;
  background: rgba(255, 122, 89, 0.12);
  border-left: 3px solid var(--primary);
  padding-left: 9px; /* compensate for border-left */
}

/* ----- Main header ----- */
.hub-app main {
  padding: 20px;
}

.hub-app .header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 16px;
}

.hub-app .title {
  font-size: 22px;
  font-weight: 700;
  color: var(--text);
}

.hub-app .badge {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  background: var(--panel);
  border: 1px solid var(--border);
  border-radius: 999px;
  padding: 6px 10px;
  color: var(--text-muted);
}

/* ----- Content container & panels ----- */
.hub-app .container {
  background: transparent;
}

/* “card/panel” style for content sections */
.hub-app .card,
.hub-app .panel,
.hub-app .container > section,
.hub-app .container > div:not(:has(nav)):not(:empty) {
  background: var(--panel) !important;
  border: 1px solid var(--border) !important;
  border-radius: 12px;
  box-shadow: 0 1px 2px rgba(16,24,40,.05);
  color: var(--text) !important;
}

/* Generic tables */
.hub-app table {
  width: 100%;
  border-collapse: separate;
  border-spacing: 0;
  color: var(--text);
}

.hub-app thead {
  background: #f3f6fa;
  color: var(--text-muted);
}

.hub-app th, .hub-app td {
  padding: 12px;
  border-bottom: 1px solid var(--border);
}

.hub-app tr:hover td {
  background: #f9fbff;
}

/* Inputs */
.hub-app input, .hub-app select, .hub-app textarea {
  background: #fff !important;
  color: var(--text) !important;
  border: 1px solid var(--border) !important;
  border-radius: 8px !important;
  padding: 8px 10px;
  outline: none;
}

.hub-app input:focus, .hub-app select:focus, .hub-app textarea:focus {
  border-color: var(--primary) !important;
  box-shadow: 0 0 0 3px rgba(255, 122, 89, .25) !important;
}

/* Buttons */
.hub-app .btn,
.hub-app button:not(.unstyled) {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: .5rem;
  padding: .5rem .9rem;
  border-radius: 8px;
  border: 1px solid transparent;
  font-weight: 600;
  cursor: pointer;
  transition: background .15s ease, color .15s ease, border-color .15s ease;
}

.hub-app .btn-primary,
.hub-app button.primary {
  background: var(--primary) !important;
  color: var(--primary-ink) !important;
}
.hub-app .btn-primary:hover,
.hub-app button.primary:hover {
  background: #ff6a44 !important;
}

.hub-app .btn-outline,
.hub-app button.outline {
  background: #fff !important;
  color: var(--text) !important;
  border-color: var(--border) !important;
}
